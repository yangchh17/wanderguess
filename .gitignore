# Personal data — never commit (your real geotagged photos!)
photoset/

# config.js is committed for deployment — its publishable key is safe to be public
# (RLS protects the data; the secret/service-role key lives only in Supabase).

# Local tooling / editor / agent
.claude/
.vscode/
.idea/

# OS cruft
.DS_Store
Thumbs.db
*.log

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
