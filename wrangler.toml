# Cloudflare Workers static-assets config.
# Serves the repo as a static site (no Worker code) — `wrangler deploy` publishes
# index.html at /, solo.html, gps-check.html, vendor/, shared/, config.js, etc.
name = "wanderguess"
compatibility_date = "2024-12-30"

[assets]
directory = "."
